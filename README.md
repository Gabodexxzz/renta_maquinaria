# Maqueta: Renta de Maquinaria Pesada (Terramax)

Esta es una **maqueta** (frontend) hecha con **HTML + CSS + JavaScript + Bootstrap 5** para una empresa de arriendo de maquinaria pesada.

## ✅ Qué incluye
- Navbar + hero (landing)
- Catálogo con **filtro por categoría** y **búsqueda**
- Modal con detalles del equipo
- Sección de servicios, planes, nosotros y contacto
- Formulario con validación (demo)
- Botón flotante de WhatsApp (demo)
- Toggle de tema (oscuro/claro)

## 📁 Estructura
```
maqueta-renta-maquinaria/
  index.html
  css/
    styles.css
  js/
    script.js
  README.md
```

## ▶️ Cómo abrir
1. Descomprime la carpeta
2. Abre `index.html` con tu navegador (Chrome/Edge/Firefox)

> No necesitas servidor. Es estático.

## 🧩 Personalización rápida
- Cambia nombre: “Terramax” en `index.html`
- Cambia teléfono/correo y enlace de WhatsApp
- Ajusta el catálogo en `js/script.js` (array `machines`)
- Ajusta colores en `css/styles.css` (variables `:root`)

## 🚀 Subir a GitHub (rápido)
1. Crea un repositorio en GitHub (vacío)
2. En tu PC, abre terminal dentro de la carpeta y ejecuta:
```bash
git init
git add .
git commit -m "Maqueta renta maquinaria"
git branch -M main
git remote add origin URL_DE_TU_REPO
git push -u origin main
```

Listo.
